# Look Into `newlib` for Embedded

Talking to @mac_gyver on Twitch and he mentioned that some are using
`newlib` for their embedded projects which is small enough to even be
used on micro-controllers.
