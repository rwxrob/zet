# Use `curl --data-urlencode` and `-G` Instead of URL

It's much cleaner to use `--date-urlencode` and `-G` instead of a really
long URL that you would have to URL encode yourself.

Tags:
    #curl #bash #urls
