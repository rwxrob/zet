# Use podman inspect to see how container created

Sometimes knowing how a container was originally created is nice (did I remember to mount a volume or port, for example). `podman inspect` shows all this.
