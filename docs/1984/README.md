# Vendoring Go mods that depend on C

> Also I found this stuff that peeps do to vendor C files alongs: go mod vendor go get -u github.com/goware/modvendor modvendor -copy="**/*.c **/*.h **/*.proto" -v
